/**
 * 1. set variable to hold inputdata - convert input to parseFloat
 * 2. set variable to hold memorydata - convert input to parseFloat
 * 3. create a switch function getNumber() for numberical input
 * 3.a create switch function for operational getOp()
 * 4. do a conditional statement concerning use of decimal "." so that it cant be used several times
 * 5. create if statement that allows use of decimal point before numerical input
 * 6. create function that displays input & memorydata
 * 7. create a clearAll() function that resets all dataput
 * 8. create a backspace function backSpace() which uses a .slice for removing last input.
 * 9.
 *
 *
 * DOM or call function onclick?
 * ? : Use class to create callbacks to functions?
 */

const numberSelector = document.getElementById("number");
const operatorSelector = document.getElementById("operator");
const backspaceSelection = document.getElementById("delete");
const outputToScreen = document.getElementById("outputScreen");
const inputToScreen = document.getElementById("inputScreen");

function getNumbers() {}

function getOperator() {}

function calculate() {}

function outPut() {}

function inPut() {}

function display() {}
